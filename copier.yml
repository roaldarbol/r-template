# questions
project_name:
  type: str
  help: What is your project name?

r_version:
  type: str
  help: Which version of R would you like to use?
  default: "4.4"

_exclude:
  - pixi.lock
  - .pixi
  - .git
  - copier.yml
  - .copier-answers.yml